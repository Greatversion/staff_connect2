import 'package:flutter/material.dart';

class TasksAssigned extends StatelessWidget {
  const TasksAssigned({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
